# Solar System Suite - Ubuntu/Debian Dependencies
# Install with: xargs -a requirements-ubuntu.txt sudo apt-get install -y

# Build essentials
cmake
build-essential
pkg-config

# Core dependencies
libssl-dev
libcurl4-openssl-dev
zlib1g-dev

# Optional: Enhanced features
libboost-dev
libcairo2-dev
libpng-dev
libgrpc++-dev
libprotobuf-dev
protobuf-compiler

# Optional: Documentation
doxygen
graphviz

# Optional: Deployment (install helm from https://helm.sh/docs/intro/install/)
# docker.io
